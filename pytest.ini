[pytest]
testpaths = tests transrouter/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --timeout=60
markers =
    live: Tests that call real APIs (Anthropic, OpenAI) — require API keys
    tier1: Fast tests with no external dependencies
    tier2: Pipeline tests that call real APIs
