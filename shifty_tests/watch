#!/bin/bash
# Simple wrapper to start the shifty test watcher

cd "$(dirname "$0")"

echo "🎤 Shifty Test Watcher"
echo "Drop .wav files into: $(pwd)"
echo ""

# Activate venv if it exists
if [ -f "../.venv/bin/activate" ]; then
    source ../.venv/bin/activate
fi

# Run the test script
python test_shifty.py "$@"
